from cayley_tables.cayley_table_actions import CayleyTableActions


def check_associativity(cayley_table_actions: CayleyTableActions) -> dict:
    associativity_info = {
        "is_associative_algebra": None,
        "non_associative_elements": [],
    }
    # Check associativity using associativity equation: a * (b * c) = (a * b) * c

    return associativity_info
